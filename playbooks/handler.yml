# handlers.yml


# this play is for rpm based distro
---
  
  hosts: web
  tasks:
    - name: Configure php.ini file
      lineinfile:
        path: /etc/php.ini
        regexp: ^short_open_tag
        line: 'short_open_tag=On'
      notify: restart apache

  handlers:
    - name: restart apache
      service: name:httpd state=restarted